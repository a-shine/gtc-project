From the Julia REPL:

julia> cd("your_path_to_julia_folder")

julia> ]

(julia) pkg> activate .

(julia) pkg> add DifferentialEquations

(julia) pkg> add DelimitedFiles

(julia) pkg> add Random

Hit backspace key

Run the driver_synthetic_gendata.jl file